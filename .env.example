# DataBossX root — environment template.
# Copy to `.env` (gitignored) and fill in real values: cp .env.example .env
# See SECURITY.md for the full setup and secret-handling rules.
# Component-specific templates: backend/.env.example, frontend/.env.example

SUPABASE_URL=
SUPABASE_KEY=
FRONTEND_URL=
BACKEND_DOCKER_URL=http://host.docker.internal:8009
MOCK_AUTH=true
