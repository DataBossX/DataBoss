# data/

Drop the workbook (`…NHE.xlsx`) and the county index PDF (`12N 24W 31 - Index.pdf`)
here, or point at them with `--workbook` / `--index`. Outputs land in `data/out/`.
Workbooks, PDFs, and outputs are git-ignored (they're client material).
