# PR #88 Evidence Index -- Viewport & Accessibility

Generated by the real Playwright/axe-core suite in `command_center/qa/`
against the actual `server.py`, running against synthetic sandbox roots
(never real Penterra/Horizon/client paths). Reproduce locally with:

```
command_center/qa/run-qa.sh
```

The full screenshot set and axe JSON for all 6 required viewports are
uploaded as CI artifacts on every run of `.github/workflows/pr88-windows-a11y.yml`
(job `viewport-a11y-qa`); this folder holds a curated subset for quick
review in the repo itself.

## Viewports tested

320x568, 375x667, 390x844, 430x932, 768x1024, 1440x900

## Results (last local run)

- Horizontal overflow (`scrollWidth <= clientWidth + 1`): PASS, all 6 viewports
- Sandbox banner (`SANDBOX MODE -- NO CLIENT DATA` + exact roots) visible: PASS, all 6
- Keyboard nav (skip link is first Tab stop, Enter activates it, Escape is a no-op): PASS, all 6
- Visible `:focus-visible` outline on focused control: PASS, all 6 (`outline-width` not `0px`)
- axe-core scan, WCAG 2.0/2.1 A+AA tags: **0 violations of any severity** on all 6 viewports (0 critical, 0 serious, 0 unlabeled form controls)
- Toast (`#toast`) and command-result (`#commandResult`) both expose `role="status"` + `aria-live="polite"`: PASS, all 6

## Files in this folder

- `screenshots/<viewport>-before-setup.png` -- full-page capture before setup confirmation, one per required viewport
- `screenshots/375x667-keyboard-focus.png` -- capture showing a visible keyboard-focus indicator
- `axe/375x667-sample.json` -- one representative full axe-core result set (all 6 are generated by CI; see workflow artifacts for the complete set)

## Source changes behind this evidence

- `command_center/static/index.html` -- skip link, `<h1>`, sandbox banner region, visually-hidden labels for the mode selector and command input, `role="status"`/`aria-live` on toast + command result
- `command_center/static/app.css` -- `:focus-visible` styles, 44px minimum touch targets, overflow-safe command bar / topbar, `prefers-reduced-motion` support
- `command_center/static/app.js` -- per-project-and-action accessible button names, sandbox banner rendering, `aria-disabled` + explanatory label on disabled write actions
