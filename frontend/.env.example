# Frontend (Create React App) environment template — copy to frontend/.env.
# NEVER commit frontend/.env. Names discovered from src/ usage.

# Base URL of the DataBossX backend API.
REACT_APP_BACKEND_URL=http://localhost:8001
