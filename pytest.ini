[pytest]
# Only collect the curated unit/smoke suite. backend_test.py is an integration
# script that requires a running server and is run manually, not in unit CI.
testpaths = tests
python_files = test_*.py
addopts = -q
