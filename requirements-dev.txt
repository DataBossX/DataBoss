# Lean dependency set to run the DataBossX unit/integration test suite.
# Heavy/native packages (paddleocr, playwright browsers, etc.) are intentionally
# excluded so CI and local test runs stay fast and reliable.

# Test & lint tooling
pytest>=8.0.0
flake8>=7.0.0

# Backend (FastAPI) runtime needed by tests/test_backend_api.py
fastapi>=0.110.1
aiosqlite>=0.20.0
httpx>=0.27.0
python-dotenv>=1.0.1
pydantic>=2.6.0
python-multipart>=0.0.9
loguru>=0.7.0

# Automation helpers exercised by unit tests
tenacity>=8.2.3
